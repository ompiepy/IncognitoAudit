export declare const MINUTE = 60000;
/**
 * Creates a Promise that resolves after a specified delay.
 * @param ms The delay duration in milliseconds.
 * @returns A Promise that resolves after the specified delay.
 * @example
 * // Wait for 1 second
 * await delay(1000);
 */
export declare const delay: (ms: number) => Promise<unknown>;
/**
 * Deletes a directory and its contents recursively.
 * @param {string} dirPath - The path to the directory to delete
 * @returns {Promise<void>} A promise that resolves when the directory is deleted
 * @private
 */
export declare const deleteDirectory: (dirPath: string) => Promise<void>;
export declare const extractHostnameAndPort: (url: string) => string;
//# sourceMappingURL=utils.d.ts.map